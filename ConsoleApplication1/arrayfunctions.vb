Module arrayfunctions
    Sub main()
        Dim list() As Integer
        Dim temp() As Integer
        'Display Original Array
        For Each i In list
            Console.WriteLine("{0}", i)
        Next
    End Sub
End Module
